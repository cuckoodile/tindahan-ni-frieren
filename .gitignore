# ───────────────────────────────────────────────────────────────
# Common / OS / Editors / Misc
# ───────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
*.swp
*.swo
*~
*.bak
*.tmp
*.log
*.tmp.*
*.cache
*.sublime-*
.vscode/
.idea/
*.code-workspace

# Environment / Secrets
.env
.env.local
.env.*.local
.env.development
.env.production
*.secret
*.key
*.pem

# ───────────────────────────────────────────────────────────────
# Python / Django (backend/)
# ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# Django / media & static (usually don't commit generated/uploaded files)
backend/media/
backend/staticfiles/
backend/*.sqlite3
backend/*.sqlite3-journal

# Coverage / testing
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
*.pytest_cache/
.tox/
.nox/
.pytest_cache/

# Django migrations (sometimes people ignore, but most keep them)
# backend/*/migrations/*.py  ← uncomment only if you really want to ignore them

# ───────────────────────────────────────────────────────────────
# JavaScript / Node / React + Vite (frontend/)
# ───────────────────────────────────────────────────────────────
node_modules/
frontend/node_modules/
frontend/.vite/
frontend/dist/
frontend/build/
frontend/.svelte-kit/
frontend/.cache/
frontend/.eslintcache
frontend/.stylelintcache

# Vite / esbuild artifacts
frontend/.temp/
frontend/tsconfig.tsbuildinfo
frontend/vite.config.*.ts.timestamp-*

# Dependency directories (sometimes people have them outside src/)
frontend/.pnp/
frontend/.pnp.js
frontend/.yarn/*
!.yarn/patches/
!.yarn/plugins/
!.yarn/releases/
!.yarn/sdks/
!.yarn/versions/

# Production / build output
frontend/dist-ssr/
frontend/ssr-manifest.json

# Logs
frontend/npm-debug.log*
frontend/yarn-debug.log*
frontend/yarn-error.log*

# Optional npm cache directory
frontend/.npm/

# TypeScript
frontend/*.tsbuildinfo

# ───────────────────────────────────────────────────────────────
# Optional: Docker, CI, deployment
# ───────────────────────────────────────────────────────────────
*.dockerignore
Dockerfile*
docker-compose*.yml
*.docker-compose.override.yml

# Deployment platforms
.vercel
.now/
.netlify/
.output/

# ───────────────────────────────────────────────────────────────
# Keep these (don't ignore)
# ───────────────────────────────────────────────────────────────
!backend/requirements*.txt
!backend/Pipfile*
!frontend/package.json
!frontend/package-lock.json
!frontend/yarn.lock
!frontend/pnpm-lock.yaml